# Suetnova-Arina-Information-technology
# Домашнее задание №1. Журнал событий на сайтах

## Сравнение двух сайтов

Для выполнения задания я использовала два сайта:

1. **Учебный сайт** (`file:///C:/Users/user/Downloads/index%20(3).html`)
2. **Сайт Яндекс Лавки** (`lavka.yandex.ru`)

---
### 1. При загрузке главной страницы

**Яндекс Лавка:**
Событие `page_view` — параметры: `dl` (адрес страницы), `dt` (заголовок страницы), `ul` (язык браузера), `sr` (разрешение экрана).

**Учебный сайт:**
Событие `page_view` — параметры: `dl` (адрес страницы), `dt` (заголовок страницы), `ul` (язык браузера), `sr` (разрешение экрана).
```
en=page_view
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/
dt=Яндекс Лавка — доставка продуктов за 15 минут
_et=3847
cid=123456789.9876543210
sid=9876543210
```
---
### 2. При просмотре списка товаров / категорий

**Яндекс Лавка:**
Событие `view_item_list` — параметры: `ep.item_list_id` (идентификатор списка товаров), `ep.item_list_name` (название списка, например «Популярное»), `epn.items_count` (количество товаров в списке).

**Учебный сайт:**
Событие отсутствует, так как на сайте нет списка товаров.
```
en=view_item_list
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/category/milk
dt=Молочные продукты — Яндекс Лавка
_et=2156
cid=123456789.9876543210
sid=9876543210
```
---
### 3. При прокрутке страницы

**Яндекс Лавка:**
Событие `scroll` — параметр: `epn.percent_scrolled` (процент прокрутки страницы).

**Учебный сайт:**
Событие `scroll` — параметр: `epn.percent_scrolled` (процент прокрутки страницы).
```
en=scroll
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/category/milk
dt=Молочные продукты — Яндекс Лавка
_et=4789
cid=123456789.9876543210
sid=9876543210
```
---

### 4. При клике на карточку товара / кнопку

**Яндекс Лавка:**
Событие `select_content` — параметры: `ep.content_type` (тип контента, например «product»), `ep.content_id` (идентификатор товара).

**Учебный сайт:**
Событие `select_content` — параметры: `ep.content_type` (тип контента, например «button»), `ep.content_id` (идентификатор кнопки, например «order_call»).
```
en=select_item
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/product/123456
dt=Молоко «Домик в деревне» 1л — Яндекс Лавка
_et=1890
cid=123456789.9876543210
sid=9876543210
```
---

### 5. При открытии карточки товара

**Яндекс Лавка:**
Событие `view_item` — параметры: `ep.item_id` (идентификатор товара), `ep.item_name` (название товара), `ep.item_category` (категория товара), `epn.price` (цена товара).

**Учебный сайт:**
Событие отсутствует, так как на сайте нет карточек товаров.
```
en=view_item
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/product/123456
dt=Молоко «Домик в деревне» 1л — Яндекс Лавка
_et=6123
cid=123456789.9876543210
sid=9876543210
```
---
### 6. При добавлении товара в корзину

**Яндекс Лавка:**
Событие `add_to_cart` — параметры: `ep.item_id` (идентификатор товара), `ep.item_name` (название товара), `epn.quantity` (количество), `epn.price` (цена).

**Учебный сайт:**
Событие отсутствует, так как на сайте нет корзины.
```
en=add_to_cart
tid=G-XXXXXXXXXX
dl=https://lavka.yandex.ru/cart
dt=Корзина — Яндекс Лавка
_et=523
cid=123456789.9876543210
sid=9876543210
```
---
## Сравнение двух сайтов

На Яндекс Лавке я зафиксировала **шесть** событий, а на учебном сайте - только **три**. Это связано с тем, что Яндекс Лавка - это полноценный интернет-магазин с длинным пользовательским путём: просмотр списка товаров, открытие карточки, добавление в корзину, оформление заказа и т.д. Каждый из этих шагов нужно отслеживать, чтобы понять, на каком этапе пользователи перестают совершать покупки. На учебном сайте путь проще: загрузка страницы, прокрутка, клик по кнопке, заполнение и отправка формы.

Источник (`ep.source`), на обоих сайтах логика одинаковая. На учебном сайте при переходе по ссылке без UTM-меток в событии `generate_lead` отображается `(not set)`. При добавлении в адрес `?utm_source=college` в событии появляется `ep.source=college`. На Яндекс Лавке аналогично: если пользователь приходит по рекламной ссылке с метками, источник заполняется, если без — попадает в `(not set)` или `direct`. Для бизнеса потеря источника — критическая проблема, так как невозможно понять, какая реклама привела клиента.

---

## Точка разрыва и план проверки

Я выбрала точку разрыва между событиями `form_start` и `generate_lead`. Это переход от начала заполнения формы к её фактической отправке.

**Именно она потому что...**
В отчёте видно 100 событий `form_start` и только 20 `generate_lead`, значит, 80 пользователей начали заполнять форму, но не отправили её. Причины могут быть разными: слишком длинная или сложная форма, непонятные поля, ошибка при отправке, сломанная кнопка. Это потеря 80% потенциальных клиентов, и такую проблему нужно выявлять и исправлять.

**Примерныыый план проверки на занятии 2: (неуверенна)**

1. Открыть учебный сайт, включить F12 → Network → фильтр `collect`.
2. Несколько раз заполнить форму, отслеживая события:
   - Начать вводить текст в поле — проверить, приходит ли `form_start`.
   - Заполнить все поля, но не нажимать «Отправить» и закрыть вкладку — проверить, что `generate_lead` не пришло (это нормально, пользователь ушёл).
   - Заполнить форму полностью и нажать «Отправить» — проверить, что пришли оба события: `form_start` и `generate_lead`.
3. Если `generate_lead` не приходит после нажатия кнопки — проблема в кнопке, валидации или коде отправки события.
4. Проверить поле `ep.source` в событии `generate_lead`:
   - Если `(not set)` — проблема с передачей UTM-меток из адресной строки.
   - Если поле отсутствует — проблема в настройках события.
